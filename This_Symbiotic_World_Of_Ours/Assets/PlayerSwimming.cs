using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSwimming : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //check if player has the ability to swim?
        //check if player hit the  edge
        //if yes set isSwimming to !isSwimming
        //if is swimming is true -> player swims, meaning can move up and down etc

    }
}
